#pragma once

namespace spaceframe {
  namespace input {

    typedef enum {
      press,
      release
    } key_state_t;

    typedef enum {
      escape,
      space,
      left_shift,
    } key_t;

  }
}
